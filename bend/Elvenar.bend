import Base

def y_sort(a: Nat, b: Nat) -> Bool:
  True{}

def render_order() -> String:
  "Elvenar — HTML5 Canvas, Y-sorted sprites, one engine for web+iOS+Android — Flash rebuilt as WebGL, pre-rendered 2D from Blender/Maya, AI for style"

def main() -> IO(Unit):
  do IO<Unit>:
    IO.print(render_order())
